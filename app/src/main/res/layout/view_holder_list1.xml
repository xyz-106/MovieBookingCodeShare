<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="180dp"
    android:layout_height="match_parent">

    <androidx.cardview.widget.CardView
        android:id="@+id/imgPikachu"
        android:layout_width="160dp"
        android:layout_height="200dp"
        android:layout_marginEnd="@dimen/margin_medium_2"
        app:cardCornerRadius="@dimen/margin_medium">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/imgMovie"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/pokemon" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/txtMovieName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/imgPikachu"
        android:layout_alignStart="@id/imgPikachu"
        android:layout_marginStart="@dimen/margin_small"
        android:layout_marginTop="@dimen/margin_small"
        android:text="Detective"
        android:textColor="@color/colorPrimaryDark"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvLength"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/txtMovieName"
        android:layout_alignStart="@id/txtMovieName"
        android:text="Mistrey/Adventure 1h 45m"
        android:textSize="@dimen/text_small" />
</RelativeLayout>